#!/bin/bash

if (( $# < 1 )); then
    f="sum"
else
    f=$1
    shift
fi

case $f in
"sum")
    prog='BEGIN{s=0} {s+=$1} END{print s}'
    ;;
"avg")
    prog='BEGIN{s=0; c=0} {s+=$1; c+=1} END{print s/c}'
    ;;
esac

awk "$prog"
